# import Pomodoro from './generated/prisma.graphql'
# import Detail from './generated/prisma.graphql'

type Query {
  sePomodoros(searchString: String): [Pomodoro!]!
}

# type Mutation {
#  deletePomodoro(id: ID!): Pomodoro
# }
